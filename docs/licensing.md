# Licensing

Trial usage is based only on successful conversions:

```text
encoder success
AND output exists
AND output size > 0
AND original size > 0
AND output size < original size
```

Phase 0 includes only local trial state. Lemon Squeezy integration is intentionally absent.
