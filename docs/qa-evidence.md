# QA Evidence

This file separates automated evidence from packaged-app manual QA. Automated
checks reduce the manual surface, but they do not replace real macOS packaged
app testing in `docs/manual-qa.md`.
Public paid beta blockers and their evidence references are tracked in
`docs/release-blockers.md`.

## Automated Coverage

| Area | Evidence | Command |
|---|---|---|
| File-size architecture | Rust production and TS/TSX limits are enforced | `cargo run -p xtask -- file-size-check` |
| External media process ban | Product sources reject `ffmpeg`, `ffprobe`, common JS media process packages, and process command construction | `cargo run -p xtask -- media-policy-check` |
| Privacy guardrails | Product sources reject default telemetry, browser analytics markers, updater enablement, and network clients outside the license provider | `cargo run -p xtask -- privacy-policy-check` |
| Release workflow shape | Release workflow gates, read-only `GITHUB_TOKEN` permissions, unsigned DMG QA artifact upload, unsigned release failure gate, CI signing certificate secret mapping, Tauri distribution metadata, CSP, updater, missing update manifests while updater is disabled, media, privacy, static site policy, `.codex`/Nix local ignore policy, parallel version manager config rejection, productization Done overclaim rejection, productization Release Gate text coverage, release blocker classification/execution-order synchronization, duplicate Execution Order track rejection, release blocker Next action detail gates, complete public web and distribution Execution Order exit evidence, secret-like files and release blocker values, public URL release blocker references without query or fragment parts, local media files, benchmark CSV path evidence, app history, release artifacts, and manual QA blocker linkage including `manual-qa-check` evidence are checked | `cargo run -p xtask -- release-check` |
| Verified blocker evidence | Verified manual QA blockers reject weak evidence for smaller outputs, original preservation, privacy receipt file-name handling, Finder selection, duplicate numbered output, no-success trial/history, numeric queue counts, verified smaller Trash output, Trash only-after ordering, Trash action state, failed activation cache observation, failed activation raw-key/fingerprint/instance absence, expired license refresh reconnect/pre-start block evidence, expired license refresh cache-observation evidence, friendly errors, unchanged trial counts, Lemon Squeezy sandbox activation with checked cache, Lemon Squeezy sandbox activation with 64-character lowercase hex fingerprint and `instance_id`, Pricing finalized production URL evidence, Homebrew uninstall/removes-cleanly Next action evidence, Published checksum SHA256SUMS Next action evidence, publish Artifact URL completion evidence, notarized stapled-status Next action evidence, Public web proof completion evidence for release-status, privacy, pricing, support, download, checkout, and refund, and distribution completion without the release notes Artifact URL | `cargo run -p xtask -- release-check` |
| Release template synchronization | Manual QA required field/check label synchronization, manual-only evidence mapping or external classification, release blockers, release blocker to release notes evidence-field mapping, release notes URL field publish-check synchronization, Execution Order to Evidence Classes synchronization, evidence classes, release notes, release docs, workflows, and the actual website directory stay covered by xtask tests | `cargo test -p xtask` |
| CI workflow shape | Pull-request CI runs format, file-size, website, release readiness, and Nix dev shell evaluation gates | `cargo run -p xtask -- release-check` |
| Security workflow shape | Security CI runs advisory, license/source, media, and privacy gates | `cargo run -p xtask -- release-check` |
| Manual QA completeness | Packaged-app evidence fields, concrete identity fields, absolute app-state paths, concrete results, malformed table rows, prepared manual QA draft markers, secret-like values, UDIF `.dmg` artifacts, same `.dmg` file evidence for artifact/checksum/signing/Gatekeeper rows, public UDIF artifact-check evidence, public signing/notary/stapler/`spctl` evidence with stapled or validate status for `DropSquash.dmg`, artifact-check/checksum/signing/Gatekeeper path evidence, non-lowercase checksums, checksum digest matching the app artifact, intended product license setup evidence, valid activation cache observation, failed activation cache observation, failed activation raw-key/fingerprint/instance absence, contradictory raw-key evidence, expired license refresh pre-start block evidence, expired license refresh cache-observation evidence, existing benchmark CSV path evidence, same-machine release candidate benchmark baseline evidence, and Gatekeeper no-warning evidence are checked | `cargo run -p xtask -- manual-qa-check` |
| Manual QA preparation | App state backup, clean git worktree preflight, repository-local and relative app-state/state/output/markdown-output path rejection, `.md` markdown-output rejection, existing markdown-output no-clobber rejection, prepared Markdown draft marker and label synchronization, trial reset, required environment metadata, canonical absolute artifact paths, release-candidate generated rows include the App artifact path and checksum digest, stale app artifact detection, canonical artifact names, and `--restore-state` are tested without deleting config by default | `cargo test -p xtask manual_qa_prepare` |
| Desktop capability policy | Main window permissions are limited to file open and Finder reveal | `cargo run -p xtask -- release-check` |
| Static site | Required pages, local links/resources including CSS `url(...)`, `srcset`, and video posters, website-root boundary rejection for local links/resources, quoted and unquoted HTML links/resources/form actions/button formactions, approved external links with host/path boundaries and no query/fragment tracking, placeholders, local-only URL hosts, secret-like values, unsupported platform availability/download/channel claims including short download CTAs and natural release copy, release-status, pricing refund link, privacy, license, refund, support contact copy, pre-release CTA copy for download/trial/checkout actions, and pre-release artifact files/resources plus download/checkout links or form actions are checked | `cargo run -p xtask -- website-check` |
| Trial counting | History accepts only successful smaller conversions, and desktop conversion rejects locked trials before starting a conversion | `cargo test -p dropsquash-history && cargo test -p dropsquash-desktop conversion` |
| CLI history writes | CLI conversion uses the same success-only history guard | `cargo test -p dropsquash && cargo test -p dropsquash-history` |
| CLI license commands | CLI reports local trial/license state, cache identity, offline grace, and raw-key absence, and removes the local cache without a raw key or network call | `cargo test -p dropsquash license && cargo run -p dropsquash -- license status --history /tmp/dropsquash-empty-history.jsonl` |
| License cache safety | Raw key persistence, raw-key field-name rejection, raw-license-key field-name rejection, Pro identity and 64-character lowercase hex fingerprint requirements, temp-file cache writes, local cache removal, expired offline-grace reconnect state, expired refresh priority over trial completion, plain and encoded error redaction, trimmed activation, failed/invalid activation raw-key/fingerprint/instance absence, and existing-cache preservation are tested | `cargo test -p dropsquash-license && cargo test -p dropsquash-desktop license` |
| Settings persistence | Output folder, profile, size, source policy, and privacy receipt preference persist through AppConfig and web saved-state mapping | `cargo test -p dropsquash-core -p dropsquash-desktop && nix develop --command pnpm --dir apps/desktop/web test` |
| Privacy receipt generation | Successful conversions create a local sidecar receipt with file names, `uploaded_bytes = 0`, and `metadata_policy = preserve`; desktop summaries expose the saved receipt path; CLI can inspect the sidecar locally | `cargo test -p dropsquash-privacy -p dropsquash -p dropsquash-desktop` |
| Source movement safety | Postprocess gates, equal-or-larger output boundaries, generated output-name ownership, and desktop command revalidation are tested | `cargo test -p dropsquash-postprocess && cargo test -p dropsquash-desktop source` |
| Queue order | Sequential queue state transitions, desktop AppState-owned queue worker commands, batch enqueue command wiring, queued job settings, active finish/failure/cancellation boundaries, queued waiting-row cancellation, clear-completed, Rust/UI batch summary, trial-lock pending-job blocking, and refresh-lock queued message preservation are tested | `cargo test -p dropsquash-queue && cargo test -p dropsquash-desktop queue && nix develop --command pnpm --dir apps/desktop/web test` |
| Cancellation token path | File stability, desktop active-conversion cancellation, temp cleanup evidence, and post-encode postprocess/history guard are tested | `cargo test -p dropsquash-fileguard && cargo test -p dropsquash-desktop state && cargo test -p dropsquash-desktop conversion` |
| Unsupported OS backends | Windows/Linux placeholders report unavailable and reject encode with a native-encoder-unavailable message instead of falling back | `cargo test -p dropsquash-encoder unimplemented_platform_backends` |
| Benchmark harness | Local encoder benchmark argument parsing, release-set sample count, `.csv` evidence output path, backend/saved-percent/duration/speed-ratio CSV fields, release-set duration and speed-ratio evidence rejection, output acceptance checks, and release blocker/manual QA linkage are tested | `cargo test -p xtask benchmark` |
| Release artifact naming | Tauri DMG output is normalized to the canonical `DropSquash.dmg` name before artifact checks, checksum generation, upload, manual QA evidence, and release notes | `cargo test -p xtask normalize_dmg` |
| Release artifact hygiene | DMG artifacts must use the canonical `DropSquash.dmg` file name and can be checked for emptiness, UDIF trailer, and `/nix/store` references | `cargo run -p xtask -- artifact-check path/to/DropSquash.dmg` |
| Release checksum | Non-empty UDIF `DropSquash.dmg` checksums can be generated deterministically, SHA256SUMS lines use the artifact file name instead of local parent directories, non-DMG targets are rejected, wrong-name targets are rejected, `/nix/store` references are rejected, `--output` must be named `SHA256SUMS`, and `--output` refuses to overwrite an existing file | `cargo run -p xtask -- checksum path/to/DropSquash.dmg --output SHA256SUMS` |
| Release notes preparation | Release notes artifact fields, release notes template field-label synchronization, required public website, pricing, refund, and live checkout URL placeholders, GitHub Release URL, SHA256SUMS line, SHA256SUMS output command, pending checksum upload placeholder, macOS verification command/evidence drafts, the matching Homebrew cask command, and the Homebrew tap PR evidence draft are generated from the checked public `DropSquash.dmg`, version-matched public Artifact URL with host/path boundaries, checksum, version, and current git commit; stale artifacts older than `HEAD` are rejected; dirty git worktrees are rejected; optional markdown output must be a new absolute `.md` path outside the repository | `cargo run -p xtask -- release-notes-prepare path/to/DropSquash.dmg <Artifact URL>` |
| Release notes evidence | Filled public release notes reject duplicate fields, placeholder URLs, placeholder checkout buy IDs, non-HTTPS URLs, URL query or fragment parts, local-only URL hosts, nested artifact URLs, Artifact URL/GitHub Release URL version mismatch, imposter public URL hosts, Pricing URL and Refund policy URL origins that differ from the Public website URL origin, non-canonical checkout hosts outside `store.lemonsqueezy.com`, secret-like values, non-lowercase Git commits, placeholder or non-lowercase checksums, prepared draft markers/placeholders, checksum evidence without public context, the Artifact URL, GitHub Release URL, or exact lowercase SHA-256 digest, Homebrew install evidence without the Artifact URL, Homebrew tap PR URL, lowercase SHA-256 digest, or clean uninstall result, checkout/website/refund mix-up, signing/notary/stapler/`spctl`, Manual QA record, and Gatekeeper evidence without the exact Artifact URL, missing public `DropSquash.dmg` signing/notary/stapler/`spctl`/Gatekeeper no-warning evidence, missing existing benchmark CSV path outside repo, missing numeric queue counts, missing waiting/queued row cancellation evidence, missing cancellation temp cleanup/no-new-success history evidence, missing verified smaller Trash output, missing Lemon Squeezy sandbox activation identity and checked-cache evidence, missing license network failure checked-cache evidence, missing expired license refresh attempted-conversion evidence, contradictory raw-key evidence, missing conversion/queue/Trash/license action-state evidence, generic verification results, vague or overclaiming limitations, and vague support contact | `cargo run -p xtask -- release-notes-check path/to/release-notes.md` |
| Publish gate | Public beta publication requires release readiness, valid static website pages, complete manual QA evidence for a UDIF public `DropSquash.dmg` matching the release notes Artifact URL with an `App build` matching current `HEAD`, release notes `SHA-256` matching the manual QA `App artifact`, release notes canonical benchmark CSV path matching manual QA, existing on disk, and staying outside the repository, filled release notes evidence without prepared draft placeholders, required release notes URL fields including Artifact URL, Public website URL, Pricing URL, Refund policy URL, Live checkout URL, GitHub Release URL, and Homebrew tap PR URL, every release blocker marked Verified with concrete Completion evidence that names the release notes Artifact URL and a traceable Evidence reference, release blocker URLs matching the release notes URLs including Pricing URL, exact `GitHub Release https://...` and `Homebrew tap PR https://...` distribution references, canonical `dropsquash.app` public web references, and host-boundary checked public publish references without query or fragment parts that point to the release notes URLs | `cargo run -p xtask -- publish-check path/to/release-notes.md` |
| Homebrew cask generation | Cask generation rejects placeholders, non-semver versions, non-GitHub release URLs, imposter GitHub release hosts, nested artifact URLs, asset URLs with query or fragment parts, mismatched release versions, non-HTTPS URLs, non-DMG URLs, wrong DMG names, non-canonical homepages, invalid checksums, placeholder checksums, non-lowercase checksums, declares no in-app auto-update, and includes the DropSquash app-state `zap` path | `cargo test -p xtask homebrew_cask` |
| macOS signing preflight | Signing preflight rejects local-only CI identities, placeholder certificates, placeholder secret values, malformed or repeated App Store Connect key ids, malformed or all-zero issuer UUIDs, missing/empty/placeholder `.p8` files, and malformed Apple team IDs | `cargo test -p xtask macos_signing_check && cargo run -p xtask -- macos-signing-check` |

## Manual-Only Evidence

These checks still require a packaged macOS app or external service state:

| Area | Why automated tests are insufficient |
|---|---|
| Choose recording conversion | Native file picker and packaged app entitlements must produce a smaller `.squashed.mp4` while preserving the original on macOS |
| Drag-and-drop conversion | Packaged WebView drag/drop behavior must produce a smaller `.squashed.mp4` while preserving the original |
| Privacy receipt sidecar | Packaged app file permissions must create the receipt next to the generated output with file names, `uploaded_bytes = 0`, and `metadata_policy = preserve` |
| Privacy receipt Finder reveal | Finder selection behavior for generated receipt sidecars must show the `.privacy.json` selected |
| Duplicate output naming | Repeated packaged-app conversions must show the second output with the user-facing `.squashed-2.mp4` numbered suffix |
| Finder reveal | Finder selection behavior must show the generated `.squashed.mp4` selected |
| Ask source policy | The post-conversion prompt must offer Trash and Keep while the original remains unchanged |
| Trash source policy | NSFileManager Trash behavior must move the original to Trash only after verified smaller output, with moving/disabled action state |
| Failed conversion | Packaged-app failure state must show a friendly error while preserving the original and trial count |
| Larger output | Packaged-app larger-output handling must show not-smaller output failure while preserving the original and trial count |
| Large-recording cancellation | User-visible timing, progress, cleanup, ready state, and no-success trial/history evidence need real media and packaged app behavior |
| Benchmark sample results | Compression ratio, duration, speed ratio, and throughput depend on private local media and target Mac hardware |
| Benchmark sample set | Release coverage needs three private short, medium, and large local recordings with smaller outputs, backend, saved percent, duration, speed ratio, and absolute CSV evidence outside the repository |
| Benchmark regression threshold | Throughput regression needs same-machine comparison against the release candidate baseline and the 20% threshold result |
| Multi-file queue | UI ergonomics and repeated drops need packaged observation of 3 queued recordings, 1 active job, sequential completion, and unrelated failure not blocking finished jobs |
| Queued job cancellation | User-visible queue cancellation needs packaged observation that the waiting row is cancelled, never starts, and trial/history has no new success |
| Batch summary | User-visible queue totals need packaged observation of numeric finished count, saved bytes, failed count, cancelled count, blocked count, and trial or license lock blocked jobs after mixed queue outcomes |
| Lemon Squeezy product setup | Requires intended product, DropSquash, sandbox product and license-key configuration evidence outside the repository |
| Lemon Squeezy sandbox purchase | Requires sandbox checkout, intended product, test buyer, and concrete order id or order number evidence outside the repository |
| Valid sandbox activation | Requires Lemon Squeezy sandbox activation, disabled submit state, Pro state, checked cache observation, 64-character lowercase hex fingerprint, `instance_id`, and raw-key absence evidence outside the repository |
| Empty key activation | Requires packaged-app disabled Activate state and local cache inspection showing no raw key, fingerprint, or instance after an empty key |
| Invalid license key handling | Requires packaged-app disabled submit state while Activating, friendly error, and inspected local cache showing no raw key, fingerprint, or instance after an invalid key |
| License network failure | Requires packaged-app friendly network error plus checked existing valid cache with 64-character lowercase hex fingerprint, `instance_id`, and raw-key absence after a failed activation request |
| Expired license refresh | Requires packaged-app UI, attempted conversion, and local cache inspection for an expired offline grace cache before conversion starts |
| Local license forget | Requires packaged-app forgetting/disabled action state, confirmed cache removal, and observed trial or locked app state after forgetting the license |
| Public website deployment | Requires the production `dropsquash.app` website URL, specifically `https://dropsquash.app/release-status`, and public release pages |
| Pricing finalized | Requires the production `dropsquash.app` pricing URL, specifically `https://dropsquash.app/pricing`, with final price copy and no draft price copy before checkout goes live |
| Refund policy finalized | Requires the production `dropsquash.app` refund policy URL, specifically `https://dropsquash.app/refund`, before checkout goes live |
| Live checkout link | Requires the live `store.lemonsqueezy.com/checkout/buy/<id>` URL for the intended product |
| Signed DMG verification | Requires the public DMG/app artifact matching the release notes Artifact URL and Developer ID signature state for the same `.dmg` file |
| Notarized/stapled DMG verification | Requires the public DMG/app artifact matching the release notes Artifact URL and Apple notary, stapler, and `spctl` assessment for the same `.dmg` file |
| Signed/notarized Gatekeeper open | Requires Developer ID signing, notarization, stapling, a clean machine, and no Gatekeeper warning for the public `DropSquash.dmg` matching the release notes Artifact URL |
| Published checksum | Requires SHA256SUMS attached to the public GitHub Release for the `DropSquash.dmg` matching the release notes Artifact URL |
| Homebrew cask install | Requires the public Homebrew tap PR and `brew install --cask` plus `brew uninstall --cask` evidence for the versioned `DropSquash.dmg` Artifact URL, matching lowercase SHA-256, `auto_updates false`, and `zap` cleanup |

Only `docs/manual-qa.md` should be used to record those manual results. Record
the exact app artifact, input sample set, and output folder so the evidence can
be reproduced later.
