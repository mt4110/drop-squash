{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window permissions for the DropSquash scaffold.",
  "windows": ["main"],
  "permissions": ["core:event:default", "dialog:allow-open", "opener:allow-reveal-item-in-dir"]
}
